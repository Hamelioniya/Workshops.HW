const apiUrl = 'http://rocket-api.belpyro.net';
const api = "api";

const urls = {
    signalRUrl: `${apiUrl}/`,
    getAllUsers: `${apiUrl}/users/all`,
    getAllRoles: `${apiUrl}/roles/all`,
    newsApiUrl: apiUrl,
};

export {
    urls as Urls,
};
